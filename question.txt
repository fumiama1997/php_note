2/24(土)　
php17-6/practice_post_code_advanced.php
・ページングの仕様は行えたので確認してもらう。
・検索結果が0件であった場合のみに水平線下に検索結果が0件という文字列のみを表示する仕様にしたい。
下記の条件分岐を行うと意図した仕様になったが条件分岐が長いためこれでよいのか質問したい。
 <?php if (empty($error) && empty($post_code_data) && ((isset($_GET['prefecture']) && isset($_GET['city'])) or isset($_GET['post_code']))) { ?>

今回は16、17章を確実にやってきたので確認を一緒に行って頂きたい。

18-19章　時間があれば確認してもらう。

20章の修正は次回。