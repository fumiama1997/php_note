一番聞きたい内容
php16-9/challenge_mysql_select.php
post送信後もセレクトボックスの選択の保持を行いたいがどのようにすればよいか分からない。

<option value="manager" <?php if ($job === 'manager') { print 'selected';} ?>>マネージャー</option>


php16-10/challenge_mysql_insert.php
  $result で結果が帰るのでそれを利用して成功失敗を分岐させればよい。

php16-11/practice_bbs_db_intermediate.php  
・ブラウザを開いただけの初期状態のときに過去発言を一覧表示する。
この仕様を行う場合どのようにコードを書けば分かりやすいのか。
今回はif (empty($goods_data))の条件を付けることで追加前のデータ表示から、追加後のデータ表示に切り替えている。

配列をいつでも通る下に書くことで配列を二個作る必要がなく、一個でシンプルにできる。



・phpMyAdminでVARCHARの値を設定しているが必ずしも必要なのか。利用者の名前、利用者のコメントの文字数制限はコードでバリデーションを行っているので作業が重複する気がする。
バリデーションはphp側　データベース側ではmyadmin どちらでも制限をしておくことで二重で確認ができる　から重複してやることが逆にいい。




今回は17章の質問はおいておく。
php17-6/practice_post_code_advanced.php
郵便番号は7桁の数値のみ検索可能とし、それ以外はエラーメッセージを表示する。
→これは全角数字でも7桁であれば検索可能とするのか

全角数字は厳密には数字ではない。半角数字のみ数字として認識されるために全角がきたらはじく半角数字のみが通る仕様にする。

郵便番号、都道府県、市区町村の入力の前後にある半角スペースを削除する。入力値チェックや検索はこの後に行う。 例)「 1100001　」→「1100001」
→これはどういうことか

半角スペースキーが入っていたら、それを削除して数字のみ見るように作っていく。



